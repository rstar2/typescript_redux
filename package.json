{
  "name": "typescript_redux",
  "version": "0.0.1",
  "scripts": {
    "watch": "webpack --watch",
    "serve": "live-server --port=8000",
    "start": "concurrently -p \"[{name}]\" -n \"HTTP,TS\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run watch\" \"npm run serve\""
  },
  "dependencies": {
    "bootstrap": "^3.3.7",
    "core-js": "^2.4.1",
    "redux": "^3.6.0"
  },
  "devDependencies": {
    "concurrently": "^3.1.0",
    "ts-loader": "^0.8.2",
    "tslint": "^4.0.2",
    "typescript": "~2.0.3",
    "webpack-dev-server": "^1.14.0"
  }
}
